# Optional: enable LLM enrichment (falls back to rule-based if unset)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-20250514

# Pipeline settings
CONFIDENCE_THRESHOLD=75
FULL_DEPTH_CATEGORY=dishwasher
